export class User {
    constructor(email, password, secretKey, wallet) {
        this.email = email;
        this.password = password;
        this.secretKey = secretKey;
        this.wallet = wallet;
    }
}
