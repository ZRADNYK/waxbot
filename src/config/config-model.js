export class Config {
    constructor(inputFile, useAlcor, useBloks, landId, useWax, useCloudWallet, updateTokens) {
        this.inputFile = inputFile;
        this.useAlcor = useAlcor;
        this.useBloks = useBloks;
        this.landId = landId;
        this.useWax = useWax;
        this.useCloudWallet = useCloudWallet;
        this.updateTokens = updateTokens;
    }
}
